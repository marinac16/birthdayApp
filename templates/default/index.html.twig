{% extends 'base.html.twig' %}

{% block title %}App{% endblock %}

{% block body %}
    <div class="container-principal">

        <div class="bonjour">
            <h1>Bonjour {{ user.name }}</h1>
        </div>


        <img src="{{asset('images/img/capitaine.png')}}" class="img-cap">

        <div class="">
            <a href="{{ path('choix') }}"> <button class="btn btn-success btn-ok" type="button">J'accepte la mission</button></a>

        </div>
    </div>


{% endblock %}